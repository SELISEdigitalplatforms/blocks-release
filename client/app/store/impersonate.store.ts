// Impersonation state is owned by blocks-kit (identical API/shape) so that its
// impersonation guards and the deployment app share a single source of truth.
export { useImpersonateStore } from "@seliseblocks/genesis-os";
